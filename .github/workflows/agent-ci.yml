name: Agent CI

on:
  pull_request:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  agent:
    name: ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-amd64
            os: ubuntu-latest
            goos: linux
            goarch: amd64
            bin: trusttwin
          - name: windows-amd64
            os: windows-latest
            goos: windows
            goarch: amd64
            bin: trusttwin.exe
          - name: macos-arm64
            os: macos-latest
            goos: darwin
            goarch: arm64
            bin: trusttwin

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache: true

      - name: Test
        run: go test ./...

      - name: Build agent
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: go build -buildvcs=false -o ${{ matrix.bin }} ./cmd/trusttwin

      - name: Upload agent binary
        uses: actions/upload-artifact@v4
        with:
          name: trusttwin-${{ matrix.name }}
          path: ${{ matrix.bin }}
          if-no-files-found: error
